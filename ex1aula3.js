// cumprimento de uma string

function stringLength(string){
    if(string === "" || typeof value === "number"){
        return;
    }
    return string.length;
}

let str = prompt("Insira uma palavra:");
let n= stringLength(str);
    alert(`A palavra tem ${n} carateres!`);

///////////////////////////////////////////////////////////////////